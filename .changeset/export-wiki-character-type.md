---
'better-wiki': patch
---

Export all public dc-fandom plugin types from the package entry point: `WikiCharacter`, `WikiCharacterHistorySection`, and `WikiFandomFlags` are now importable directly from `'better-wiki'`.

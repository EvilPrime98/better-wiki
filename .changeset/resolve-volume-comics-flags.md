---
'better-wiki': patch
---

`WikiVolume.getComics` and `resolveVolumeComics` now accept and forward `thumbnailSize`, `includeCollections`, and `category`, in addition to `sorted`, matching the flags supported by `getComic`.
